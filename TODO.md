# reclip — открытые вопросы до реализации

> Рабочее имя проекта — `reclip` (см. вопрос 0). Менеджер истории буфера обмена
> для Linux (GNOME/Wayland), аналог `Win+V`. Язык — Rust, GUI — GTK4.
> Этот файл — список того, что надо решить **до** написания кода.
> Каждый вопрос обсуждаем отдельно, решение переносим в `docs/`.

Статусы: ⬜ не обсуждали · 🟨 обсуждаем · ✅ решено (ответ в `docs/`)

---

## 0. Организационное
- ✅ **0.1 Имя проекта.** `reclip`. → `docs/00-organizational.md`
- ✅ **0.2 Лицензия.** MIT. → `docs/00-organizational.md`
- ✅ **0.3 Репозиторий.** Отдельный git, `git init` сейчас, заливка на GitHub позже. → `docs/00-organizational.md`
- ✅ **0.4 Язык документации и кода.** Доки/комментарии — русский; идентификаторы — английские. → `docs/00-organizational.md`

## 1. Что именно ловим и храним (модель данных)
- ⬜ **1.1 Типы содержимого в MVP.** Подтверждаем: только текст. Как в модели заложить будущие картинки?
- ⬜ **1.2 Две «выделения» X11/Wayland.** Следим только за CLIPBOARD (Ctrl+C) или ещё за PRIMARY (выделение мышью)?
- ⬜ **1.3 Дубликаты.** Скопировал то же самое второй раз — новая запись, или поднимаем старую наверх?
- ⬜ **1.4 Пустое/мусорное.** Игнорируем пустой буфер и строки из одних пробелов?
- ⬜ **1.5 Лимит размера одной записи.** Обрезаем гигантские вставки? До скольки?
- ⬜ **1.6 Сколько записей храним.** Максимум N штук? Значение по умолчанию? Что при переполнении (вытесняем старые)?

## 2. Хранилище истории
- ⬜ **2.1 Где лежит файл.** По стандарту XDG: `~/.local/share/reclip/`? `~/.cache/`? `~/.config/`?
- ⬜ **2.2 Формат файла.** JSON / SQLite / своё? (важно для будущих картинок и надёжности)
- ⬜ **2.3 Живучесть между перезагрузками.** История сохраняется после reboot? (ожидаемо да)
- ⬜ **2.4 Одновременный доступ.** Демон пишет, пикер читает один файл — как не поймать гонку/порчу файла?
- ⬜ **2.5 Версионирование формата.** Как не сломать старую сохранённую историю при обновлении программы?

## 3. Демон (фоновое слежение)
- ⬜ **3.1 Интервал поллинга.** Как часто заглядываем в буфер (мс)? Баланс отзывчивость/нагрузка.
- ⬜ **3.2 Как запускается.** systemd user-сервис / автозапуск `.desktop` / вручную? Что для MVP?
- ⬜ **3.3 Один экземпляр.** Как не дать запуститься двум демонам сразу?
- ⬜ **3.4 Способ чтения буфера.** Крейт (`wl-clipboard-rs`/`arboard`) или вызов `xclip`? Что берём и почему?

## 4. Пикер (окно выбора по `Super+V`)
- ⬜ **4.1 Горячая клавиша.** Настраиваем через «Custom Shortcut» GNOME, запускающий `clip show`. Какая комбинация по умолчанию?
- ⬜ **4.2 Что делает выбор.** Только кладём в буфер, или ещё авто-вставляем (эмуляция `Ctrl+V`)? (авто-вставка на Wayland сложна)
- ⬜ **4.3 Поведение окна.** Обычное окно по центру; закрывается по Esc/потере фокуса/выбору?
- ⬜ **4.4 Навигация.** Стрелки + Enter; нужен ли поиск-фильтр по набору букв уже в MVP?
- ⬜ **4.5 Как выглядит.** Минимальный список строк vs. карточки с превью. Что в MVP?

## 5. Связка демон ↔ пикер
- ⬜ **5.1 Проблема «эха».** Пикер кладёт запись в буфер → демон видит это как новое копирование. Как не задваивать/не перемешивать историю?
- ⬜ **5.2 Канал общения.** Хватит ли общего файла для MVP, или нужен локальный сокет уже сейчас?

## 6. Сборка, установка, распространение
- ⬜ **6.1 Установка для друга.** `cargo install` / готовый бинарь в релизах GitHub / `install.sh`? Что для MVP?
- ⬜ **6.2 Системные зависимости.** GTK4 и (возможно) `xclip` — как их наличие проверять/подсказывать пользователю?
- ⬜ **6.3 Офлайн/прокси сборка.** Учитываем окружение с proxychains — не будет ли проблем с загрузкой крейтов?
- ⬜ **6.4 Конфиг пользователя.** Нужен ли файл настроек (интервал, размер истории, клавиша)? Формат и расположение?

## 7. Структура кода
- ⬜ **7.1 Раскладка проекта.** Один бинарь + внутренняя lib: как разложить (cargo workspace? один crate с `lib.rs` + `main.rs`?).
- ⬜ **7.2 Trait-абстракция буфера.** Финальная форма «переходника» для будущих X11/Wayland/Windows реализаций.

---

## Отложено на будущее (НЕ в MVP, только фиксируем, чтобы не забыть)
- 🔮 Поддержка картинок
- 🔮 Закрепление (pin) избранных записей
- 🔮 Распознавание типа содержимого (ссылка, e-mail, код, цвет…)
- 🔮 Приватность: маскирование паролей, режим «не сохранять»
- 🔮 Кроссдистрибутивность (не только Ubuntu/GNOME)
- 🔮 Поддержка Windows (в самую последнюю очередь)
