# 00. Организационные решения

Решения по группе 0 из `TODO.md`. Дополняется по мере обсуждения.

## 0.1 Имя проекта — ✅ `reclip`
Итог: **`reclip`** (от *re-clip* / *recall clipboard* — «вернуть из буфера»).
Коротко, отражает суть. Папка проекта: `reclip/`.

## 0.4 Язык документации и кода — ✅ русский
Итог: **документация и комментарии — на русском.**
- README, файлы в `docs/`, `TODO.md`, комментарии в коде — по-русски.
- Идентификаторы в коде (имена функций, переменных, типов) — по устоявшейся практике
  оставляем **английскими** (так принято в Rust и удобно для инструментов), но
  поясняющие комментарии к ним — русские.

## 0.2 Лицензия — ✅ MIT
Итог: **MIT** — максимально простая и популярная, минимум обязательств,
никого не отпугивает. Файл `LICENSE` создан. Копирайт: `2026 Sergey (Kartavichh)`.

## 0.3 Репозиторий — ✅ отдельный git, init сейчас
Итог:
- **Отдельный** git-репозиторий (не связан с SIGame).
- `git init` выполнен **сейчас, до кода** — история начинается с проектных документов
  (практика «документ-первой»).
- Имя на GitHub — `reclip`, публичный. **Заливка на GitHub — позже**, пока локально.
